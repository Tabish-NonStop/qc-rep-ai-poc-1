nextflow.enable.dsl = 2

docker {
    enabled = true
    //runOptions = '--add-host=host.docker.internal:host-gateway'
}

params {
    reads  = "${projectDir}/data/dataset.fastq"
    outdir = "${projectDir}/results"
}